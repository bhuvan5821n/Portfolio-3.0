const repo = 'https://github.com/bhuvan5821n/Friday-and-Jarvis/blob/main/';
export const fridayChapters = [
  {id:'why',label:'Why',title:'Less switching. More continuity.',body:'A question, a file, a browser and a reminder often belong to the same task. FRIDAY explores how a personal assistant can bring conversation and computer actions into one workspace.',detail:'The motivation in my public profile is personal: an assistant built around the way I think and use my computer. This remains a desktop prototype, with implementation documented in the public repository.',source:'README.md'},
  {id:'architecture',label:'Architecture',title:'One workspace. Separate responsibilities.',body:'The Python application brings a PyQt6 interface together with voice sessions, model access, memory and action modules. Each part has a specific job: present the state, interpret a request, retain context or perform an action.',detail:'The repository separates core/, actions/, memory/ and Studios/. The diagram summarizes those responsibilities; it is an architectural reading of the source, not a claim that every studio is complete.',source:'README.md'},
  {id:'routing',label:'AI routing',title:'Choose a path for the task.',body:'Text and vision requests use an OmniRoute adapter. A keyword classifier selects a task category; an attached image selects a vision route. A saved manual override can pin the model family.',detail:'The adapter defines fallback candidates and exposes routing status to the interface. Native audio sessions and search grounding follow separate Gemini paths. Routing support in code does not establish that a provider was connected in the supplied capture.',source:'core/ai.py'},
  {id:'voice',label:'Voice',title:'Listening is a system of its own.',body:'The voice path captures microphone audio, queues it for a Gemini Live session and handles spoken output. When muted, the input callback checks a local wake detector instead of placing microphone blocks in the outgoing audio queue.',detail:'main.py includes an optional SpeechCore capture bridge with a Python audio fallback. Device selection handles Windows audio API differences. A microphone and configured services are required to test the complete voice experience.',source:'main.py'},
  {id:'memory',label:'Context & memory',title:'Context that can carry forward.',body:'The memory manager stores categorized facts locally and selects entries for the assistant’s prompt. Identity, preferences, projects, relationships, wishes and notes have different roles in that context.',detail:'The formatter ranks entries, limits categories and caps the assembled prompt text. Local persistence does not mean all context stays offline: selected memory is formatted for model use. Remember and forget operations are explicit functions in the memory manager.',source:'memory/memory_manager.py'},
  {id:'tools',label:'Local commands & tools',title:'A response is not an action.',body:'Some paths are deterministic. The model adapter answers recognized creator-identity questions locally before requesting a model. Other requests can lead to structured tool calls, dispatched to separate action modules.',detail:'The voice dispatcher records a returned tool call as executed rather than verified complete. Returning from a function does not prove that an app opened or an external state changed.',source:'main.py'},
  {id:'automation',label:'Automation',title:'Give the task a life beyond the chat.',body:'The reminder module creates a notification script and schedules it through Windows Task Scheduler. It uses a time trigger and a least-privilege task definition, allowing the operating system to own the scheduled action.',detail:'Scheduler errors are handled explicitly. Browser and computer actions depend on the machine, installed applications and session state. A visible Automation button alone is not evidence of a complete automation studio.',source:'actions/reminder.py'},
  {id:'iteration',label:'Performance & iteration',title:'Make the work observable.',body:'The routing adapter exposes the last decision and writes request metrics. It also resizes and compresses image inputs before sending them to a model. These mechanisms make behavior inspectable; they are not published latency or reliability results.',detail:'The interface capture shows an idle control center waiting for its first routed request. The source describes later implementation details. Those records have their own boundaries and are not a synchronized benchmark.',source:'core/ai.py'},
  {id:'challenges',label:'Challenges',title:'The difficult edges are between systems.',body:'Audio devices, remote providers and desktop tools can fail independently. The source addresses Windows output API compatibility and capture-engine disconnection.',detail:'These are documented engineering constraints, not a reconstructed personal account of development. No recovery-rate claim is inferred from the presence of a fallback.',source:'main.py'},
  {id:'decisions',label:'Decisions',title:'Keep the boundaries visible.',body:'The implementation separates resettable user memory from creator identity and groups tools by responsibility. Those boundaries make different requirements easier to reason about.',detail:'Windows is the primary documented desktop target. The public interface names more studios than this record can establish as operational, so the case study distinguishes interface evidence from implementation evidence.',source:'README.md'},
  {id:'learning',label:'Learning & next questions',title:'The next record should be a tested workflow.',body:'The implementation raises a useful question: how should an assistant show the difference between an attempted action and a verified result? A personal retrospective from Bhuvan has not yet been recorded.',detail:'A useful next addition would follow one repeatable task from request to verified result, including failures and recovery. That is a proposed evaluation, not a completed test or a claim about personal learning.',source:'README.md'},
] as const;
export const fridaySource = (path:string) => repo + path;
